# Dependensi untuk Streamlit Community Cloud.
# torch sengaja TIDAK dipin ketat: biar pip memilih wheel CPU yang cocok
# dengan versi Python yang dipilih di Cloud (hindari mismatch wheel).
streamlit==1.58.0
chromadb==1.5.9
groq==1.4.0
sentence-transformers==5.5.1
wikipedia==1.4.0
torch
